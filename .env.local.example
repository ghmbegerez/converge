# Converge — Local Development Mode
# Copy to .env and source before running: source .env
#
# This disables auth, rate limiting, and runs with SQLite (default).
# All features work; only external integrations (GitHub) need extra config.

# --- Core ---
# CONVERGE_DB_PATH=.converge/state.db        # SQLite path (default)

# --- Auth (disabled for local dev) ---
CONVERGE_AUTH_REQUIRED=0
# CONVERGE_AUTH_MODE=apikey                   # apikey | jwt | hybrid
# CONVERGE_API_KEYS=mykey:admin:dev::*        # key:role:actor:tenant:scopes

# --- Rate limiting (disabled for local dev) ---
CONVERGE_RATE_LIMIT_ENABLED=0
# CONVERGE_RATE_LIMIT_RPM=60                  # requests per minute

# --- GitHub integration (optional) ---
# CONVERGE_GITHUB_APP_ID=
# CONVERGE_GITHUB_APP_PRIVATE_KEY_PATH=
# CONVERGE_GITHUB_WEBHOOK_SECRET=
# CONVERGE_GITHUB_INSTALLATION_ID=

# --- Worker (optional, for automated queue processing) ---
# CONVERGE_WORKER_POLL_INTERVAL=5
# CONVERGE_WORKER_BATCH_SIZE=20
# CONVERGE_WORKER_MAX_RETRIES=3
# CONVERGE_WORKER_TARGET=main
# CONVERGE_WORKER_AUTO_CONFIRM=0
# CONVERGE_WORKER_SKIP_CHECKS=1
